import { environment } from 'src/environments/environment';

export class SystemConstants {
    public static SERVER = environment.APIBaseURL;
    public static CLIENT = 'http://localhost:4200/#';
}
